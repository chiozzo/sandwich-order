sandwich.arrayToDropdown(sandwich.getBreads());
sandwich.arrayToDropdown(sandwich.getCheeses());
sandwich.arrayToDropdown(sandwich.getMeats());
sandwich.arrayToDropdown(sandwich.getSauces());
sandwich.arrayToDropdown(sandwich.getVeggies());
